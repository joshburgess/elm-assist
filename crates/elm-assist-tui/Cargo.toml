[package]
name = "elm-assist-tui"
version = "0.4.0"
edition = "2024"
description = "Interactive terminal dashboard for the elm-assist toolchain"
license = "MIT OR Apache-2.0"

[lib]
name = "elm_assist_tui"
path = "src/lib.rs"

[[bin]]
name = "elm-assist-tui"
path = "src/main.rs"

[dependencies]
ratatui = "0.29"
crossterm = "0.28"
tokio = { version = "1", features = ["rt-multi-thread", "macros", "sync", "time"] }
notify = { version = "7", features = ["macos_fsevent"] }
rayon = "1"
serde_json = "1"
elm-ast = "0.2.1"
elm-lint = { path = "../elm-lint", version = "0.4.0" }
elm-unused = { path = "../elm-unused", version = "0.4.0" }
elm-deps = { path = "../elm-deps", version = "0.4.0" }
elm-search = { path = "../elm-search", version = "0.4.0" }
elm-refactor = { path = "../elm-refactor", version = "0.4.0" }

[dev-dependencies]
test-better = "0.2.2"
